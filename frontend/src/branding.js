/** Public URL for the AppliCache wordmark + tagline lockup (served from `public/`). */
export const BRANDING_LOGO_LOCKUP_SRC = "/applicache_logo_wcaption.png";

/** Accessible description of the lockup (matches on-image text). */
export const BRANDING_LOGO_LOCKUP_ALT =
  "AppliCache — Cache applications, Catch opportunities";
